# v1.0.6 API Documentation and Mock Store Closeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the unfinished local v1.0.6 work by documenting its changes from v1.0.5, aligning API documentation with the mock-store migration, and removing the two retired analytics Storage fallbacks.

**Architecture:** Business truth remains centralized in `utils/mock/mock-store.js` while `USE_MOCK=true`; each state slice maps to a future backend endpoint. Device/runtime state, authentication tokens, onboarding flags, debug switches, and UI preferences remain valid direct Storage users.

**Tech Stack:** WeChat Mini Program JavaScript/WXML/WXSS, Node.js test runner, static HTML technical documentation.

---

### Task 1: Remove retired analytics Storage fallbacks

**Files:**
- Modify: `utils/util.js`
- Test: `tests/repository-hygiene.test.js`

- [x] Add a failing repository hygiene test for the retired `userInfo` and `student` reads.
- [x] Run the test and confirm it fails on both legacy reads.
- [x] Read profile data from `globalData`, character gender from `character-gender`, and membership from `membership`.
- [x] Run the focused test and confirm it passes.

### Task 2: Publish the v1.0.6 delta in the API document

**Files:**
- Modify: `docs/mini-app-tech-doc.html`

- [x] Change the document delivery version/date to v1.0.6 / 2026-06-25.
- [x] Add an explicit “v1.0.6 compared with v1.0.5” change summary.
- [x] Add the 13-slice mock-store → backend endpoint mapping table.
- [x] Replace stale “business state still uses scattered Storage” descriptions with the new single mock-store layer.
- [x] Preserve the distinction between business truth and valid client/device Storage.

### Task 3: Regenerate and verify the share document

**Files:**
- Regenerate: `docs/mini-app-tech-doc.share.html`

- [x] Run the focused migration and documentation checks.
- [x] Run the document share build.
- [x] Run the complete Node test suite and report unrelated baseline failures separately.
- [x] Review `git diff` for accidental edits and summarize the v1.0.5 → v1.0.6 delivery.
