page {
  height: 100%;
  background: #fbfaf5;
  color: #101010;
}

.sub-page {
  height: 100%;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  background: #fbfaf5;
  font-family: PingFang SC, PingFangSC-Regular, sans-serif;
}

.nav-title {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 700;
  pointer-events: none;
}

.sub-scroll {
  flex: 1;
  min-height: 0;
  box-sizing: border-box;
  padding: 18rpx 31rpx 0;
}

/* ---- 顶部邀请卡 ---- */
.invite-hero {
  position: relative;
  overflow: hidden;
  padding: 40rpx 32rpx 32rpx;
  border-radius: 32rpx;
  background: linear-gradient(135deg, #2f80ed 0%, #56a8ff 60%, #7cc3ff 100%);
  color: #ffffff;
  box-shadow: 0 16rpx 36rpx rgba(47, 128, 237, 0.25);
}

.hero-kicker {
  font-size: 20rpx;
  font-weight: 700;
  letter-spacing: 3rpx;
  opacity: 0.85;
}

.hero-title {
  margin-top: 8rpx;
  font-size: 40rpx;
  font-weight: 800;
  line-height: 1.4;
}

.hero-title + .hero-title {
  margin-top: 0;
}

.hero-sub {
  margin-top: 14rpx;
  font-size: 24rpx;
  line-height: 1.6;
  opacity: 0.9;
}

.hero-sub-em {
  font-weight: 800;
}

.code-card {
  display: flex;
  align-items: center;
  margin-top: 26rpx;
  padding: 22rpx 26rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.16);
  border: 1rpx solid rgba(255, 255, 255, 0.35);
}

.code-copywrap {
  flex: 1;
  min-width: 0;
}

.code-label {
  font-size: 22rpx;
  opacity: 0.85;
}

.code-value {
  margin-top: 4rpx;
  font-size: 44rpx;
  font-weight: 800;
  letter-spacing: 6rpx;
  font-family: Menlo, Consolas, monospace;
}

.code-copy-btn {
  flex: none;
  padding: 12rpx 30rpx;
  border-radius: 999rpx;
  background: #ffffff;
  color: #2f80ed;
  font-size: 24rpx;
  font-weight: 800;
}

.code-copy-btn-hover {
  opacity: 0.85;
}

.hero-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 26rpx;
}

.hero-action {
  flex: 1;
  height: 88rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 800;
  box-sizing: border-box;
}

.hero-action-poster {
  background: #ffffff;
  color: #2f80ed;
  box-shadow: 0 10rpx 24rpx rgba(23, 64, 111, 0.18);
}

/* button 复位：转发按钮与普通视图按钮观感一致 */
.hero-action-share {
  margin: 0;
  padding: 0;
  line-height: 88rpx;
  background: rgba(255, 255, 255, 0.18);
  border: 2rpx solid rgba(255, 255, 255, 0.55);
  color: #ffffff;
  border-radius: 24rpx;
}

.hero-action-share::after {
  border: none;
}

.hero-action-hover {
  opacity: 0.85;
}

/* ---- 卡片 ---- */
.invite-card {
  margin-top: 22rpx;
  padding: 26rpx 26rpx 10rpx;
  background: #ffffff;
  border: 1rpx solid rgba(240, 237, 229, 0.95);
  border-radius: 24rpx;
  box-shadow: 0 10rpx 24rpx rgba(47, 96, 150, 0.045);
}

.invite-card-title {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  font-size: 28rpx;
  font-weight: 800;
  color: #111318;
}

.invite-card-note {
  font-size: 22rpx;
  font-weight: 400;
  color: #8b9098;
}

.inviter-row,
.invitee-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 22rpx 0;
}

.invitee-row + .invitee-row {
  border-top: 1rpx solid #f2f0ea;
}

.person-avatar {
  flex: none;
  width: 68rpx;
  height: 68rpx;
  border-radius: 50%;
  background: #f3f5f8;
}

.person-avatar-empty {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
}

.person-copy {
  flex: 1;
  min-width: 0;
}

.person-name {
  font-size: 27rpx;
  font-weight: 700;
  color: #111318;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.person-meta {
  margin-top: 4rpx;
  font-size: 21rpx;
  color: #8b9098;
}

.invite-badge {
  flex: none;
  padding: 6rpx 18rpx;
  border-radius: 999rpx;
  font-size: 21rpx;
  font-weight: 700;
}

.invite-badge-success {
  background: #e6f9f0;
  color: #0fa968;
}

.invite-badge-pending {
  background: #fff4e5;
  color: #e8890c;
}

.invite-empty {
  padding: 30rpx 0 36rpx;
  font-size: 24rpx;
  color: #8b9098;
  text-align: center;
}

.invite-rules {
  margin-top: 22rpx;
  padding: 0 10rpx;
  font-size: 21rpx;
  line-height: 1.7;
  color: #9aa3b5;
}

.invite-rules-strong {
  font-weight: 700;
  color: #5a6678;
}

.scroll-spacer {
  height: 60rpx;
}

/* ---- 海报弹窗 ---- */
.poster-dialog-mask {
  position: fixed;
  z-index: 110;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(17, 19, 24, 0.66);
}

.poster-dialog {
  position: relative;
  width: 600rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.poster-frame {
  width: 600rpx;
  height: 960rpx;
  border-radius: 24rpx;
  overflow: hidden;
  background: #2f80ed;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 24rpx 60rpx rgba(0, 0, 0, 0.3);
}

.poster-image {
  width: 600rpx;
  display: block;
}

.poster-loading {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.85);
}

.poster-actions {
  display: flex;
  gap: 24rpx;
  margin-top: 30rpx;
  width: 100%;
}

.poster-action {
  flex: 1;
  margin: 0;
  height: 92rpx;
  line-height: 92rpx;
  border-radius: 24rpx;
  font-size: 30rpx;
  font-weight: 800;
  color: #ffffff;
  padding: 0;
}

.poster-action::after {
  border: none;
}

.poster-action-save {
  background: #ff9f43;
}

.poster-action-send {
  background: #19c37d;
}

.poster-action-hover {
  opacity: 0.85;
}

.poster-close {
  position: absolute;
  top: -76rpx;
  right: 0;
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  border: 2rpx solid rgba(255, 255, 255, 0.85);
  color: #ffffff;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 离屏画布：仅用于导出海报图片 */
.invite-poster-canvas {
  position: fixed;
  top: -9999px;
  left: -9999px;
  width: 600px;
  height: 960px;
}
